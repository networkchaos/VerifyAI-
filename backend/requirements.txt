# Python dependencies for face detection models
# Install with: pip install -r requirements.txt

# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
scipy>=1.4.1

# YOLOv8 Face Detection (Ultralytics) - Recommended, fast and accurate
ultralytics>=8.0.0

# MediaPipe Face Detection - Lightweight, Google's solution
mediapipe>=0.10.0

# DeepFace - Multiple face recognition backends (VGG-Face, Facenet, etc.)
deepface>=0.0.79

# Face Recognition - dlib-based, very accurate
# Note: Requires dlib which may need Visual C++ on Windows
# face-recognition>=1.3.0

# InsightFace - Modern alternative to RetinaFace (no TensorFlow dependency)
insightface>=0.7.3

